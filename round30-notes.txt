Round 30 Investigation Notes:

Issue 1: Invoice not generated after approving payroll
- Root cause: Payroll approve mutation does NOT auto-trigger invoice generation
- Invoice generation is manual via the InvoiceGenerationPanel on Invoice page
- The user approved GB Feb 2026 payroll but didn't click Generate on Invoice page
- The generation panel exists but user may not know to use it
- Fix: Keep manual generation, but ensure it works. Also add manual invoice creation.

Issue 2: Invoice sorting
- listInvoices in db.ts sorts by createdAt desc - this is fine
- But the type filter dropdown is missing new types (monthly_eor, monthly_visa_eor, monthly_aor, visa_service, manual)
- Fix: Update type filter options

Issue 3: PDF 2 pages
- The footer is placed at doc.page.height - 40 which is the LAST page
- But pdfkit creates a new page if content doesn't fill the first page and footer is placed on that new page
- Root cause: The footer writes to a fixed Y position on the CURRENT page, but if content ends mid-page, pdfkit may have already advanced
- Actually looking at the code more carefully: the footer is at line 302, placed at doc.page.height - 40
- This should be on the same page as content. The issue might be that content is close to bottom, triggering a page break, then footer goes on page 2
- Fix: Add footer to EVERY page using doc.on('pageAdded') or check if we need to render footer differently

Issue 4: Subtotal calculation
- In createGroupInvoice (line 383): totalSubtotal += costInSettlement + taxAmount  <-- BUG!
- Subtotal should be pre-tax: totalSubtotal += costInSettlement (without tax)
- Then total = subtotal + serviceFee + tax
- Also line 415: totalServiceFee += totalFee + feeTax <-- BUG! serviceFee should be pre-tax too
- Fix: subtotal = sum of employment costs (pre-tax), serviceFeeTotal = sum of service fees (pre-tax), tax = total tax, total = subtotal + serviceFeeTotal + tax

Issue 5: Exchange rate
- getExchangeRate is called in createGroupInvoice
- Need to check exchangeRateService.ts

Issue 6: Manual invoice creation
- Invoice create mutation exists (line 122 in invoices.ts)
- But no UI button for manual creation
- Fix: Add "New Invoice" button with dialog

Issue 7: Billing entity payment terms
- Need to check schema and remove paymentTermDays

Issue 9: Billing entity country/currency selectors
- Need to change text inputs to dropdowns

Issue 10: EUR countries
- Need to check countries_config for EUR mapping
